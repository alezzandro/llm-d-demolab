apiVersion: v1
kind: Service
metadata:
  name: prefix-cache-lab
  namespace: prefix-cache-lab
  labels:
    app: prefix-cache-lab
spec:
  selector:
    app: prefix-cache-lab
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
