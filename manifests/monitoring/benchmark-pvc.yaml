apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: benchmark-data
  namespace: models-as-a-service
  labels:
    app.kubernetes.io/part-of: llm-d-demolab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
