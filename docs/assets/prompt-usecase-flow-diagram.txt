Generate a clear, professional flow diagram and architecture image
(landscape, 16:9, white background, 300 DPI print quality) that illustrates
the end-to-end use case of this conference booth demo:
"Governed multi-tenant LLM inference with prefix-cache optimization."

Use the Red Hat color palette: Red (#EE0000) accents, dark gray (#333333)
text, light warm gray (#F5F5F5) background panels. Flat modern style, no 3D.
Label every arrow with protocol, direction, and purpose. Use solid arrows for
live runtime paths and dashed arrows for admin/provisioning or conceptual
future paths.

OVERALL LAYOUT — three horizontal bands (top to bottom) plus a narrow
left sidebar and right sidebar inside one rounded outer frame labeled
"Conference Booth Use Case — llm-d + MaaS on OpenShift AI 3.5":

═══════════════════════════════════════════════════════════════════
LEFT SIDEBAR — "Model Lifecycle" (vertical, dashed admin path only)
═══════════════════════════════════════════════════════════════════

Draw as a DASHED vertical chain (not on the runtime request path):

  Box 1: "Red Hat AI Model Catalog"
    Label: "Validated Llama 3.1 8B Instruct FP8 ModelCar"
    ↓ dashed arrow: "register version + OCI URI"
  Box 2: "Model Registry v1.5"
    ↓ dashed arrow: "deploy LLMInferenceService (4 replicas)"
  Box 3: "LLMInferenceService llama-3-1-8b-fp8"
    Label: "KServe / llm-d — prefix caching ON"
    ↓ dashed arrow: "expose via MaaSModelRef"
  Box 4: "MaaS subscriptions + API keys"
    Label: "devspaces-subscription + chatbot-subscription"

Caption at bottom of sidebar: "Operator-driven setup (one-time, not per request)"

═══════════════════════════════════════════════════════════════════
TOP BAND — "Consumers" (user-facing applications)
═══════════════════════════════════════════════════════════════════

Three consumer boxes side by side at the TOP of the main canvas:

1. PATH A — "Developer in Dev Spaces + Continue AI" (blue accent)
   Icon: person at laptop
   Badge: devspaces-subscription | 50k tokens/h | engineering-tools
   Outbound labels:
     • "HTTPS POST /v1/chat/completions"
     • "Bearer <devspaces-api-key>"
     • "model: llama-3-1-8b-instruct-fp8"

2. PATH B — "Ops engineer in Open WebUI" (orange accent)
   Icon: chat bubble
   Badge: chatbot-subscription | 100k tokens/h | customer-support
   Same outbound labels but "Bearer <chatbot-api-key>"

3. PATH C — "Booth presenter in Prefix Cache Lab" (green accent)
   Icon: bar chart in browser
   Badge: reuses chatbot API key | unique vs shared TTFT microbench
   Same MaaS endpoint as PATH B

All three paths merge downward into ONE labeled URL:
  https://maas.<cluster-domain>/models-as-a-service/llama-3-1-8b-fp8/v1

═══════════════════════════════════════════════════════════════════
MIDDLE BAND — "MaaS Gateway + LLM Serving" (live runtime path)
═══════════════════════════════════════════════════════════════════

Show a left-to-right SOLID chain for the live booth v1 request path:

  Step 1: "maas-default-gateway" (Gateway API)
    ↓ "HTTPRoute from LLMInferenceService router"
  Step 2: "Authorino"
    ↓ "validate API key → MaaSSubscription + MaaSAuthPolicy"
  Step 3: "Kuadrant"
    ↓ "enforce token rate limit (50k or 100k / hour)"
  Step 4: "Kubernetes Service (llama-3-1-8b-fp8-kserve)"
    ↓ "round-robin Service LB (booth v1 live path)"
  Step 5: Shared GPU pool — four boxes in a row:
    "vLLM replica 1 / L4" | "vLLM replica 2 / L4" |
    "vLLM replica 3 / L4" | "vLLM replica 4 / L4"
    Badge on each: "--enable-prefix-caching"

PATH A detail — highlight replica 2 with a green glow:
  Label: "shared system prompt → KV cache HIT on warm replica
         (prefix caching within replica; live path still uses Service LB)"

PATH B detail — highlight replica 4 with a neutral selection:
  Label: "ops chat prompt → any replica via round-robin"

Return path — SOLID arrows back upward from selected replica:
  "JSON completion → Service → HTTPRoute → Gateway → consumer"
  Side note: "MaaS records token usage against subscription"

CONCEPTUAL OVERLAY (DASHED, do NOT merge with solid path):
  To the right of the GPU pool, show a dashed box group:
    "Endpoint Picker (EPP) + KV Cache Indexer"
    Sub-labels: prefix-cache-scorer, queue-scorer, active-request-scorer
  Dashed arrows from EPP toward replicas labeled:
    "future llm-d routing — prefix-aware replica stickiness
     (leave-behind; InferencePool not on live MaaS path in booth v1)"

═══════════════════════════════════════════════════════════════════
BOTTOM BAND — "Platform & Governance" (foundation)
═══════════════════════════════════════════════════════════════════

Foundation boxes spanning the full width (thin upward support arrows to
middle band, NOT request-path arrows):

  • "Red Hat OpenShift 4.22" — Routes, RBAC, SCCs
  • "OpenShift AI 3.5 DataScienceCluster" — KServe, MaaS, Model Registry
  • "NVIDIA GPU Operator + NFD" → upward to each L4 under vLLM replicas
  • "Perses / User Workload Monitoring" — dashed scrape arrows from
    vLLM PodMonitor (:8000) and DCGM exporter

Label: "4× AWS g6.2xlarge — 1× NVIDIA L4 per replica"

═══════════════════════════════════════════════════════════════════
RIGHT SIDEBAR — "Booth Beat — Prefix Cache Lab Results"
═══════════════════════════════════════════════════════════════════

Browser mockup of the Prefix Cache Lab UI:

  Chart 1 — live booth result:
    "Unique Prefixes" (tall red TTFT bars) vs
    "Shared Prefixes" (short green TTFT bars)
    Callout: "~23–32% TTFT improvement (live)"

  Chart 2 — canned EPP leave-behind (dashed border around chart):
    "Naive LB vs llm-d @ concurrency 64"
    Bars: "24,391 ms" (red) vs "478 ms" (green)
    Callout: "98% P95 TTFT reduction (showroom numbers)"

Caption: "Live chart = prefix caching effect; dashed chart = EPP routing story"

═══════════════════════════════════════════════════════════════════
LEGEND + FOOTER
═══════════════════════════════════════════════════════════════════

Mini legend (bottom-right corner):
  Solid arrow  = live runtime request/response (booth v1)
  Dashed arrow = admin setup or conceptual EPP routing
  Blue path    = Dev Spaces consumer
  Orange path  = Open WebUI consumer
  Green path   = Prefix Cache Lab

Footer strip:
  Left: Red Hat logo + "OpenShift AI 3.5"
  Center: "Same GPUs. Governed access. Optimized tail latency."
  Right: "4× NVIDIA L4 — Llama 3.1 8B FP8 — OpenShift 4.22"

OVERALL STYLE:
  Enterprise conference poster quality. Consumers at TOP, platform at BOTTOM.
  Do NOT draw EPP on the same solid line as Authorino → Service → vLLM.
  Arrows must be clean, labeled, and readable at slide scale.
